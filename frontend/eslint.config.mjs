import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  {
    rules: {
      // Downgraded from error to warn: the codebase intentionally calls
      // setState inside effects for legitimate client-only patterns —
      // localStorage hydration, prefers-reduced-motion guards before setting
      // up IntersectionObservers, and fetch-on-mount data loading. These are
      // safe and conventional, so they shouldn't fail the build.
      "react-hooks/set-state-in-effect": "warn",
    },
  },
  // Override default ignores of eslint-config-next.
  globalIgnores([
    // Default ignores of eslint-config-next:
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
  ]),
]);

export default eslintConfig;
